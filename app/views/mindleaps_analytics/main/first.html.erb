<head>
  <script src="http://code.highcharts.com/highcharts.js"></script>
</head>
<h1>Main#first</h1>
<p>Find me in app/views/mindleaps_analytics/main/first.html.erb</p>
<!--<table>-->
<%# @students.each do |student| %>
<%# id = student.id %>
<%# dob = student.dob #nr_of_grades = Grade.where(student_id: id).count %>
<!--<%# age = @now.year - dob.year - ((@now.month > dob.month || (@now.month == dob.month && @now.day >= dob.day)) ? 0 : 1) %>-->
<!--<%# time = ((Time.now - student.created_at).to_i) / (24 * 60 * 60) %>-->
<!--&lt;!&ndash;<tr>&ndash;&gt;-->
<!--&lt;!&ndash;<td><%#= id %></td>&ndash;&gt;-->
<!--&lt;!&ndash;<td><%#= age %></td>&ndash;&gt;-->
<!--&lt;!&ndash;<td><%#= time %></td>&ndash;&gt;-->
<!--&lt;!&ndash;</tr>&ndash;&gt;-->

<%# end %>

<!--</table>-->

<p>
<div id="chart" style="width:100%; height:400px;"></div>
</p>

<script>
    $(function () {
        var myChart = Highcharts.chart('chart', {
            chart: {
                type: 'scatter'
            },
            title: {
                text: 'Performance in time'
            },
            xAxis: {
                title: {
                    text: 'Nr. of Lessons'
                }
            },
            yAxis: {
                title: {
                    text: 'Performance'
                }
            },
            series: [{
                name: 'Group A',
                data: <%= @seriesA %>
            }]
        });
    });
</script>
